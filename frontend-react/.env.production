# Production API Configuration
# Use relative paths so the frontend uses the same origin (https://segrd.com)
# and the Nginx proxy at /api/ routes to the backend at :9000
VITE_API_URL=/api
VITE_API_BASE_URL=/api
VITE_API_KEY=mcp-forensics-api-key-change-me

# WebSocket Configuration
# Will be set dynamically in runtime to match protocol (ws:// or wss://)
VITE_WS_URL=/ws

# App Configuration
VITE_APP_NAME=SEGRD Forensics
VITE_APP_VERSION=4.5.0
